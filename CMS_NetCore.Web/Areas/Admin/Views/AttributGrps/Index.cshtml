@model IEnumerable<CMS_NetCore.DomainClasses.AttributGrp>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attr_type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attr_type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductGroup.AliasName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AttributGrpId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AttributGrpId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AttributGrpId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
